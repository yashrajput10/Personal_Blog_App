<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Crud</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <!-- Custom CSS -->
    <style>
        body {
            color: rgb(128, 128, 128);
            background-color: #74a1d4;
        }

        .card {
           border: none;
        }

        .card-title{
            color: #0b6bdf;
        }

        .card-subtitle{
            color: #74bae5;
        }

        .card-text {
            color: #515860;
        }

        .card-img-top {
            width: 100%;
            height: auto;
            object-fit: cover;
        }

        /* Add hover effect to the card */
        .card:hover {
            cursor: pointer;
            box-shadow: 0 0 15px rgba(231, 223, 223, 0.3);
        }
    </style>
</head>

<body>
    <div class="container mt-5">
        <div class="row">
            <% blogs.forEach((blog, index)=> { %>
            <div class="col-3 md-4 mb-4">
                <a href="/" style="text-decoration: none; color: inherit;">
                    <div class="card bg-white text-white">
                        <!-- Image -->
                        <img src="img/blogs.jpg" class="card-img-top" alt="Blog Image">
                        <div class="card-body">
                            <h5 class="card-title"><strong>Title:</strong>
                                <%= blog.title %>
                            </h5>
                            <h6 class="card-subtitle mb-2"><strong>Name:</strong>
                                <%= blog.blogName %>
                            </h6>
                            <p class="card-text"><strong>Type:</strong>
                                <%= blog.type %>
                            </p>
                            <p class="card-text"><strong>Description:</strong>
                                <%= blog.description %>
                            </p>
                        </div>
                    </div>
                </a>
            </div>
            <% }) %>
        </div>
    </div>
</body>

</html>
