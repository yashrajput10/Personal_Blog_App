<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Crud</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <!-- Custom CSS -->
    <style>
        body {
            color: white;
            background-color: #96c0dd;
        }

        .card {
            border: none;
        }

        .card-title{
            color: #0b6bdf;
        }

        .card-subtitle{
            color: #74bae5;
        }

        .card-text {
            color: #515860;
        }

        .btn-edit {
            background-color: hsl(191, 99%, 49%);
            padding-left: 10px;
            padding-right: 10px;
        }

        .btn-delete {
            background-color: #ff0707;
        }

        /* Responsive images */
        .card-img-top {
            width: 100%;
            height: auto;
            object-fit: cover;
            
        }
    </style>
</head>

<body>
    <div class="container mt-5">
        <div class="row">
            <% blogs.forEach((blog, index)=> { %>
                <div class="col-3 md-4 mb-4">
                    <a href="/" style="text-decoration: none; color: inherit;">
                        <div class="card bg-white text-white">
                            <!-- Image -->
                            <img src="img/blogs.jpg" class="card-img-top" alt="Blog Image">
                            <div class="card-body">
                                <h5 class="card-title"><strong>Title:</strong>
                                    <%= blog.title %>
                                </h5>
                                <h6 class="card-subtitle mb-2"><strong>Name:</strong>
                                    <%= blog.blogName %>
                                </h6>
                                <p class="card-text"><strong>Type:</strong>
                                    <%= blog.type %>
                                </p>
                                <p class="card-text mb-2"><strong>Description:</strong>
                                    <%= blog.description %>
                                </p>
                                <a href="/editblog/<%= blog.id %>" class="btn btn-danger btn-sm btn-edit">Edit</a>
                                <a href="/deleteblog/<%= blog.id %>"
                                    class="btn btn-warning btn-sm btn-delete">Delete</a>
                                <a class="btn btn-light btn-sm" href="/form">Add Blog</a>
                            </div>
                        </div>
                    </a>
                </div>
                <% }) %>
        </div>
    </div>
</body>

</html>